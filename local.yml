---
# file: local.yml
#   Short-cut to run local install tasks without setting up an inventory.
# 
# Run roles locally via following command:
#   ansible-playbook -i localhost, local.yml [-e var=val]* --tags=<tag[,tag]> --skip-tags=<tag[,tag]>
- hosts: localhost
  connection: local
  tasks:
  - include_tasks: tasks/slurm.yml
    tags: slurm
  - include_tasks: tasks/pbs.yml
    tags: pbs
  - include_tasks: tasks/nginx.yml
    tags: nginx
  - include_tasks: tasks/proftpd.yml
    tags: proftpd
  - include_tasks: tasks/supervisor.yml
    tags: supervisor
